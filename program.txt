# В одной строке может быть только одна метка
# В одной строке читается только одна операция, разделенная пробелами, все остальное игнорируется

START: NOP  # Пустая операция
jmp skip
        NOP   NOP  # Еще одна пустая операция

   skip:
        HLT     # Завершение программы

# Конец программы